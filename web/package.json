{
  "name": "p4kvm-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp dist/index.html ../main/index.html && cp public/favicon.ico ../main/favicon.ico",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "vite": "^8.0.11",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
